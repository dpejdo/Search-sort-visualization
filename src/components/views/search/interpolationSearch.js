import { Grid } from '../../template/grid/grid';
import { interpolationSearch } from '../../../utilites/search';
export const InterpolationSearchPage = () => {
  return <Grid func={interpolationSearch} label={'Interpolation search'} />;
};
